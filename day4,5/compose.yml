version: '3.8'
services:
  app:
    image: crashoo/summertxt-app:latest
    platform: linux/amd64
    ports:
      - "3000:3000"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    volumes:
      - ./data:/data
    stdin_open: true
    tty: true